import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pokédex | Pokedex",
  description: "A site with detailed Pokémon data.",
};

export default function Pokedex() {
  return <div>to be continued...</div>
}